Pandas Doc:
    https://pandas.pydata.org/docs/reference/index.html

To run the Jupyter notebook (correctly :) ):
    Python version 3.7.1 or above is REQUIRED
    pip install jupyter
    pip install pandas
    pip install matplotlib
    pip install pyarrow

Do not forget to replace paths for csv files in Movies dataset 